[template]
prefix = "/network_metadata"
src = "bird.tmpl"
dest = "/etc/bird/bird.conf"
owner = "root"
mode = "0644"
keys = [
  "nodes",
  "racks",
]
check_cmd = "birdc status"
reload_cmd = "birdc reload"