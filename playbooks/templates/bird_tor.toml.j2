[template]
prefix = "/network_metadata"
src = "bird_tor{{rack_no}}.tmpl"
dest = "/etc/bird/bird_tor{{rack_no}}.conf"
owner = "root"
mode = "0644"
keys = [
  "nodes",
  "racks",
]
check_cmd = "birdc status"
reload_cmd = "birdc reload"