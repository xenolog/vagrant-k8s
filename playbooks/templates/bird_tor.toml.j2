[template]
prefix = "/network_metadata"
src = "bird_tor{{"%02d"|format(rack_number)}}.tmpl"
dest = "/etc/bird/bird_tor{{"%02d"|format(rack_number)}}.conf"
owner = "root"
mode = "0644"
keys = [
  "nodes",
  "racks",
]
check_cmd = "birdc status"
reload_cmd = "birdc reload"